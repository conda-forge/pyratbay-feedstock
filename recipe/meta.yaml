{% set name = "pyratbay" %}
{% set version = "2.0.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyratbay-{{ version }}.tar.gz
  sha256: ab2234abcd79df5fcc5de24963b967cc402dfcbfcb63bf7fbc10d680dc26762a

build:
  number: 0
  skip: true  # [win]
  entry_points:
    - pbay = pyratbay.__main__:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - numpy
    - pip
    - python
    - setuptools
  run:
    - matplotlib-base >=3.9.2
    - mc3 >=3.2.0
    - numpy >=2.0.0
    - scipy >=1.13.1
    - chemcat >=0.3.12
    - h5py >=3.10.0
    - python

test:
  requires:
    - pytest >=3.9
    - mpi4py >=3.1.4
  imports:
    - pyratbay

about:
  home: https://github.com/pcubillos/pyratbay
  summary: Python Radiative Transfer in a Bayesian Framework
  license: GPL-2.0-only
  license_file: LICENSE
  dev_url: https://github.com/pcubillos/pyratbay
  doc_url: https://pyratbay.readthedocs.io/en/latest/index.html

extra:
  recipe-maintainers:
    - pcubillos
